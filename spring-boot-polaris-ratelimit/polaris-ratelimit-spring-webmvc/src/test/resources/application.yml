polaris:
  address: grpc://127.0.0.1:8091
  discovery:
    register:
      enable: true
  rate-limit:
    enable: true
    labels-prefix-key:  x-polaris-
server:
  port: 8888
spring:
  application:
    name: rate-limit
